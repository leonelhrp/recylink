export enum EventCategory {
  WORKSHOP = 'workshop',
  MEETUP = 'meetup',
  TALK = 'talk',
  SOCIAL = 'social',
}

export enum EventStatus {
  DRAFT = 'draft',
  CONFIRMED = 'confirmed',
  CANCELLED = 'cancelled',
}
